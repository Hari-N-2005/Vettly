# Used for local Docker image builds if you want to override default.
# The docker-compose file already sets VITE_API_URL=/api as build arg.
VITE_API_URL=/api
VITE_API_TIMEOUT=30000
VITE_ENVIRONMENT=production
